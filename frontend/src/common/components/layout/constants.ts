const maxWidth = {
  desktop: 800,
}
export const defaultBoxHeight = 310
export const maxWidthDesktop = maxWidth.desktop

const height = {
  header: {
    desktop: 70,
    mobile: 56,
  },
  navigation: {
    desktop: 50, // For desktop only
  },
}

export const headerHeightDesktop = height.header.desktop
export const headerHeightMobile = height.header.mobile
export const navigationHeightDesktop = height.navigation.desktop
